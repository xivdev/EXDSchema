name: WKSMissionReward
fields:
  - name: Item
    targets: [Item]
    type: link
  - name: ExpModifier
    type: array
    count: 3
    comment: "tmp = (((1374389535 * ExpToNex * (lvl < 50 ? index0 : lvl < 90 ? index1 : index2)) >> 32) >> 5); exp = tmp + (tmp >> 31)"
  - name: CosmoCredits
  - name: PlanetCredits
  - type: array
    count: 3
    name: ResearchReward
  - name: ItemCount
  - name: Unknown19
    comment: Needs to match WKSEmergencyProblem.Unknown2 to be active?
  - name: Tool
    type: array
    count: 3
    fields:
      - type: link
        targets: [WKSCosmoToolClass]
  - name: TypeIndex
    type: array
    count: 3
