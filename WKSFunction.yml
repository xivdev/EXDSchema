name: WKSFunction
fields:
  - name: RequiredQuests0
    comment: "Required quest needed for function to return true"
    type: array
    count: 4
    fields:
      - type: link
        targets: [Quest]
  - name: RequiredQuests1
    comment: "Required quest needed for function to return true"
    type: array
    count: 4
    fields:
      - type: link
        targets: [Quest]
  - name: RequiredQuests2
    comment: "Best guess for now as no references are easily found in the game files"
    type: array
    count: 4
    fields:
      - type: link
        targets: [Quest]
  - name: RequiredDevGrade
    comment: "Needs this grade to be completed for function to be valid. Game checks for WKSManager.DevGrade < RequiredDevGrade[]"
    type: array
    count: 4
    fields:
      - type: link
        targets: [WKSDevGrade]
  - name: Unknown12
    comment: "Seems to only control one function tree for now relating to something in WKSManager but is also slated to check for '- 2 >= 2' and fail"
    type: array
    count: 4
