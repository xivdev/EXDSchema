name: SecretRecipeBookGroup
fields:
  - name: SecretRecipeBook
    comment: "Index is CraftType, also used as bit index in PlayerState.UnlockedSecretRecipeBooks"
    type: array
    count: 8
    fields:
      - type: link
        targets: [SecretRecipeBook]